05/10 
建立conda環境 & MySQL安裝設定

05/11
已完成 planner.py (planner node)

建立 MySQL 資料庫格式指令

使用已建立好的 MySQL 使用者載入 .sql 檔案: (位置記得換)
mysql -h 127.0.0.1 -P 3306 -u ics_agent -p < /home/hislab/Intelligent-Customer-Service-Agent/schema.sql

專案帳號登入:
mysql -h 127.0.0.1 -P 3306 -u ics_agent -p intelligent_customer_service

測試看看有沒有:
SHOW TABLES;
SELECT * FROM customers;
SELECT * FROM orders;


05/13
新增STM功能
  - 建立 LangGraph 的 MemorySaver checkpointer
  - 產生同一輪對話用的 session_id & thread_id 設定
  - checkpointer 包含 recent messages , tool outputs
  - 接起來讓 planner , react 都吃的到 stm
  - planner 部份是先用 messages 傳進去之後，用 format_recent_messages 理解成簡短的文字，再放入 planner 的 prompt